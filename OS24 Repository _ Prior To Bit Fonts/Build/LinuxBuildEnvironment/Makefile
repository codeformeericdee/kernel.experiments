# This will call all the processes from here.
# There are other make files, but this one handles them.

# For testing: 
test:
	$(info The UNIX build environment was set up successfully.)



# For the build environment:
16Bits_OS24:
	cd ../../AutomatedSoftwareCompilation/LinuxDevelopmentEnvironment && $(MAKE) $@